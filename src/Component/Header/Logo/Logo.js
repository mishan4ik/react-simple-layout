import React from "react"

let Logo = (props) => {

   return(

    <div>

                <div className = "block__Logo">
                    <img src={props.logo} alt="" />
                </div>

    </div>

   ) 



}
export default Logo;
